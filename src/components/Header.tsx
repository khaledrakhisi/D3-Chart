import React from "react";

import NavBar from "./NavBar";

import "./Header.module.scss";

const Header = () => {
  return (
    <header>
      <NavBar />
    </header>
  );
};

export default Header;
